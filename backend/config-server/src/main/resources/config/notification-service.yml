server:
  port: 9060

spring:
  application:
    name: notification-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}

security:
  jwt:
    secret: "MySuperSecretKeyForJwt1234567890MyExtra"

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /docs

vonage:
  api-key: 0e391ea0
  api-secret: ZBW((n1v*GFYkP6
  from: OlioNova